[tool.poetry]
name = "poker-player"
version = "0.1.0"
description = "Training AI poker player"
authors = ["Philip Bretz"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
